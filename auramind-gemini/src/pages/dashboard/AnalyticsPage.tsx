import AnalyticsDashboard from '../../components/dashboard/AnalyticsDashboard';

export default function AnalyticsPage() {
  // AnalyticsDashboard already wraps itself in PageShell internally
  return <AnalyticsDashboard />;
}
